export enum Token {
    JWT_TOKEN = 'JWT_TOKEN',
    GOOGLE_ACCESS_TOKEN = 'GOOGLE_TOKEN'
}

export enum User {
    USER_NAME = 'USER_NAME',
    USER_ROLE = 'USER_ROLE'
}

export enum Cookie {
    COOKIE_TIMEOUT_DAY = 1,
    COOKIE_TIMEOUT_SECOND = 60 * 60 * 24,
    COOOKIE_TIMEOUT_DELETE = 0,
}

export enum Shipping {
    SHIPPING_NOTE = 'SHIPPING_NOTE',
    SHIPPING_ADDRESS = 'SHIPPING_ADDRESS'
}